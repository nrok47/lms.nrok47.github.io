# 📚 LMS Prototype - Learning Management System

## 📋 ภาพรวมโครงการ

โครงการนี้เป็นการสร้าง **UI/UX ของ Learning Management System (LMS)** แบบ Prototype พร้อมฟีเจอร์หลักได้แก่:

- ✅ **โครงสร้างหลักสูตร (Module Structure)** - แสดงโครงสร้างการเรียน พร้อมวิดีโอ
- ✅ **แบบทดสอบแบบโต้ตอบ (Quiz Component)** - ตรวจคำตอบทันที พร้อมคะแนนและ Feedback
- ✅ **ควบคุมการเข้าถึง (Access Control)** - จำลอง Freemium Model
- ✅ **เลือกหลักสูตร (Course Selector)** - เลือกคอร์สตัวอย่างและสลับมุมมอง
- ✅ **ฟอรั่มจำลอง (Forum Simulation)** - คำถามและคอมเมนต์

---

## 🎯 วัตถุประสงค์

เป้าหมายของโครงการนี้คือ:
1. สร้าง UI/UX ที่ดีและใช้งานง่าย
2. ทดสอบฟังก์ชันหลักของ LMS
3. เตรียมสำหรับการทดสอบกับกลุ่มเป้าหมาย (Tribe) ได้ทันที
4. ไม่ต้องกังวลเรื่องการตั้งค่า Backend ที่ซับซ้อน

---

## 📁 โครงสร้างไฟล์โครงการ

```
lms.nrok47.github.io/
├── src/
│   ├── components/
│   │   ├── ModuleStructure.jsx      # Component สำหรับแสดงโครงสร้างหลักสูตร
│   │   ├── QuizComponent.jsx        # Component สำหรับแบบทดสอบ
│   │   ├── AccessControl.jsx        # Component สำหรับจำลอง Freemium
│   │   └── CourseSelector.jsx       # หน้าเลือกหลักสูตร (3 คอร์สตัวอย่าง)
│   ├── styles/
│   │   ├── ModuleStructure.css
│   │   ├── QuizComponent.css
│   │   ├── AccessControl.css
│   │   └── CourseSelector.css
│   │   └── App.css
│   ├── App.jsx                      # Component หลัก
│   └── main.jsx
├── public/
│   └── index.html
├── package.json
├── vite.config.js
└── README.md                        # ไฟล์นี้
```

---

## 🚀 การติดตั้งและรันโครงการ

### 1️⃣ ติดตั้ง Dependencies

```bash
npm install
```

### 2️⃣ รันโครงการในโหมด Development

```bash
npm run dev
```

แอปพลิเคชันจะเปิดที่ `http://localhost:5173` (หรือพอร์ตอื่นถ้า 5173 ถูกใช้ไป)

### 3️⃣ Build สำหรับ Production

```bash
npm run build
```

---

## 📖 คำแนะนำการพัฒนาฟังก์ชันหลัก

### **A. การจำลอง Freemium (Access Control)**

#### 📝 ตำแหน่งไฟล์
[src/components/AccessControl.jsx](src/components/AccessControl.jsx)

#### 🎯 วัตถุประสงค์
จำลองการล็อกเนื้อหาพรีเมียม เพื่อให้ผู้ใช้ Free เห็นว่าต้องซื้อเพื่อเข้าถึงบางส่วน

#### 💻 ขั้นตอนการพัฒนา

**Step 1: สร้าง State สำหรับสถานะผู้ใช้**
```javascript
const [isFreeUser, setIsFreeUser] = useState(true);
```

**Step 2: ออกแบบ Conditional Rendering**
```javascript
if (canAccess) {
  // แสดงเนื้อหา Module 1 & 2
  return <div className="module-content">...</div>;
} else {
  // แสดง "ซื้อเพื่อเข้าถึง"
  return <div className="locked-content">...</div>;
}
```

**Step 3: สำหรับแต่ละ Module กำหนด isPremium**
```javascript
const modules = [
  {
    id: 1,
    title: 'Module 1: Micro-learning Basics',
    isPremium: false,  // ผู้ใช้ Free เห็นได้
    // ...
  },
  {
    id: 3,
    title: 'Module 3: Live Workshop Content',
    isPremium: true,   // ผู้ใช้ Free เห็นไม่ได้
    // ...
  }
];
```

#### ✨ ฟีเจอร์ที่มีแล้ว
- ✅ State `isFreeUser` ตั้งค่าเป็น `true`
- ✅ Conditional Rendering เพื่อแสดง/ซ่อนเนื้อหา
- ✅ ข้อความ "ซื้อเพื่อเข้าถึง" สำหรับ Module 3
- ✅ ปุ่มสลับระหว่าง Free และ Premium User
- ✅ Embed YouTube Videos ผ่าน iFrame

#### 🧪 วิธีทดสอบ
1. เปิดแท็บ "🔐 ควบคุมการเข้าถึง"
2. ในตอนแรกคุณจะเห็น Module 1 & 2 ได้ แต่ Module 3 ล็อก
3. คลิก "อัปเกรดเป็นพรีเมียม" เพื่อเห็นการเปลี่ยนแปลง
4. ตอนนี้ Module 3 จะเห็นได้

---

### **B. การพัฒนา Quiz (QuizComponent)**

#### 📝 ตำแหน่งไฟล์
[src/components/QuizComponent.jsx](src/components/QuizComponent.jsx)

#### 🎯 วัตถุประสงค์
สร้างแบบทดสอบแบบโต้ตอบที่ตรวจคำตอบทันที พร้อมคะแนนและ Feedback

#### 💻 ขั้นตอนการพัฒนา

**Step 1: สร้าง Data Structure สำหรับคำถาม**
```javascript
const quizData = [
  {
    id: 1,
    question: 'คำถาม?',
    options: ['ตัวเลือก 1', 'ตัวเลือก 2', ...],
    correctAnswer: 0,  // ดัชนีของคำตอบที่ถูก
    explanation: 'อธิบาย...'
  },
  // ...
];
```

**Step 2: สร้าง State สำหรับติดตามสถานะ**
```javascript
const [currentQuestion, setCurrentQuestion] = useState(0);
const [score, setScore] = useState(0);
const [selectedAnswers, setSelectedAnswers] = useState({});
const [showScore, setShowScore] = useState(false);
```

**Step 3: สร้าง Event Handler สำหรับตรวจคำตอบ**
```javascript
const handleAnswerClick = (index) => {
  const isCorrect = index === quizData[currentQuestion].correctAnswer;
  
  if (isCorrect) {
    setScore(score + 1);
    setFeedback('✅ ถูกต้อง!');
  } else {
    setFeedback('❌ ไม่ถูกต้อง');
  }
  
  setAnswered(true);
};
```

**Step 4: แสดงผลคะแนนและ Feedback**
```javascript
if (showScore) {
  return (
    <div className="score-section">
      <h2>ผลการทดสอบ</h2>
      <p>คุณตอบถูก {score} ข้อ จากทั้งหมด {quizData.length}</p>
      // แสดงบทสรุปรายละเอียด
    </div>
  );
}
```

#### ✨ ฟีเจอร์ที่มีแล้ว
- ✅ Array of Objects เก็บคำถาม-คำตอบ
- ✅ ฟอร์มเลือกคำตอบ
- ✅ Client-side Validation แบบ Real-time
- ✅ แสดงผลคะแนนทันที
- ✅ คำอธิบายสำหรับคำตอบที่ถูก/ผิด
- ✅ บทสรุปรายละเอียดของคำตอบทั้งหมด
- ✅ ความคิดเห็นบนพื้นฐานคะแนน (Excellent, Good, Fair, Poor)

#### 🧪 วิธีทดสอบ
1. เปิดแท็บ "📝 แบบทดสอบ"
2. อ่านคำถามและเลือกคำตอบ
3. ดูข้อความ Feedback ที่ปรากฏทันที
4. ดำเนินการต่อไปยังคำถามถัดไป
5. เมื่อตอบเสร็จทั้งหมด จะเห็นผลลัพธ์และบทสรุป

#### 💡 วิธีเพิ่มคำถามใหม่
เพียงแค่เพิ่มเข้าไปในอาร์เรย์ `quizData`:
```javascript
{
  id: 6,
  question: 'คำถามใหม่?',
  options: ['ก', 'ข', 'ค', 'ง'],
  correctAnswer: 0,
  explanation: 'ความอธิบาย'
}
```

---

### **C. การจำลอง Forum (Forum Simulation)**

#### 📝 ตำแหน่งไฟล์
[src/components/ModuleStructure.jsx](src/components/ModuleStructure.jsx) - ส่วน Forum

#### 🎯 วัตถุประสงค์
สร้าง Forum ง่ายๆ เพื่อจำลองการถาม-ตอบและคอมเมนต์ ในตอนนี้ข้อมูลจะเก็บใน State เท่านั้น

#### 💻 ขั้นตอนการพัฒนา

**Step 1: สร้าง State สำหรับเก็บโพสต์**
```javascript
const [forumPosts, setForumPosts] = useState([]);
const [newPost, setNewPost] = useState('');
```

**Step 2: สร้าง Form สำหรับป้อนข้อความ**
```javascript
<form onSubmit={handleForumSubmit}>
  <textarea 
    placeholder="พิมพ์คำถามหรือคอมเมนต์..."
    value={newPost}
    onChange={(e) => setNewPost(e.target.value)}
  />
  <button type="submit">ส่ง</button>
</form>
```

**Step 3: สร้าง Event Handler สำหรับส่งข้อมูล**
```javascript
const handleForumSubmit = (e) => {
  e.preventDefault();
  
  const post = {
    id: Date.now(),
    author: 'ผู้ใช้',
    content: newPost,
    timestamp: new Date().toLocaleString('th-TH')
  };
  
  setForumPosts([...forumPosts, post]);
  console.log('📝 โพสต์ใหม่:', post); // Log ไป Console
  setNewPost(''); // Clear form
};
```

**Step 4: แสดงรายชื่อโพสต์**
```javascript
{forumPosts.map(post => (
  <div key={post.id} className="forum-post">
    <strong>{post.author}</strong>
    <span>{post.timestamp}</span>
    <p>{post.content}</p>
  </div>
))}
```

#### ✨ ฟีเจอร์ที่มีแล้ว
- ✅ Form สำหรับป้อนคำถาม/คอมเมนต์
- ✅ เก็บข้อมูลใน State (Client-side) หรือเชื่อม Firestore ถ้าตั้งค่า Firebase
- ✅ แสดงรายชื่อโพสต์ที่ส่งมา
- ✅ Log ไปที่ Console เพื่อให้เห็นการโต้ตอบ
- ✅ การเก็บเวลาและผู้ใช้

#### 🧪 วิธีทดสอบ
1. เปิดแท็บ "📖 โครงสร้างหลักสูตร"
2. คลิกเพื่อขยาย Module ใดก็ได้
3. เลื่อนลงไปที่ส่วน "💬 ฟอรั่ม"
4. พิมพ์คำถาม/คอมเมนต์ และคลิก "ส่งคำถาม / คอมเมนต์"
5. โพสต์จะปรากฏด้านล่าง
6. เปิด Developer Console (F12) เพื่อดูรายละเอียดใน Console

#### 💡 วิธีขยายฟีเจอร์ Forum
ในอนาคตสามารถเพิ่มได้:
```javascript
// เพิ่มฟังก์ชันตอบกลับ
const handleReply = (postId) => {
  // ตรรมชาติการตอบกลับ
};

// เพิ่มฟังก์ชัน Like
const handleLike = (postId) => {
  // นับการ Like
};

// เชื่อมต่อกับ Backend API
const submitPostToServer = async (post) => {
  const response = await fetch('/api/forum/posts', {
    method: 'POST',
    body: JSON.stringify(post)
  });
  return response.json();
};
```

---

### **D. ตัวเลือกหลักสูตร (Course Selector)**

#### 📝 ตำแหน่งไฟล์
[src/components/CourseSelector.jsx](src/components/CourseSelector.jsx)

#### 🎯 วัตถุประสงค์
ให้ผู้ใช้เลือก “หลักสูตรตัวอย่าง” จากรายการ (เช่น 3 คอร์สสาธิต) และเมื่อเลือกแล้ว จะตั้งค่าหลักสูตรที่เลือกไว้ในแอป และสลับไปยังแท็บ "📖 โครงสร้างหลักสูตร" โดยอัตโนมัติ เพื่อดูโมดูลของคอร์สนั้นทันที

#### 💻 การทำงานโดยสรุป
- มีรายการคอร์สสาธิต 3 คอร์ส พร้อมรายละเอียดสั้น ๆ
- คลิกคอร์ส → บันทึกคอร์สที่เลือกใน State ของแอป (`selectedCourse`) และสลับแท็บไปที่ Modules
- หน้า Modules จะแสดงชื่อคอร์สที่เลือกที่ส่วนหัว หากไม่เลือก จะแสดงหัวข้อเริ่มต้น

#### 🧪 วิธีทดสอบ
1. เปิดแท็บ "🎯 เลือกหลักสูตร"
2. คลิกคอร์สตัวอย่างใดก็ได้
3. แอปจะพาไปยังแท็บ "📖 โครงสร้างหลักสูตร" อัตโนมัติ และแสดงชื่อคอร์สที่เลือก

#### 💡 การเพิ่มคอร์สใหม่ (อย่างย่อ)
เพิ่มอ็อบเจ็กต์ในรายการคอร์สภายใน `CourseSelector.jsx` เช่น
```javascript
{
  id: 'c4',
  title: 'ชื่อหลักสูตรใหม่',
  instructor: 'ชื่อผู้สอน',
  description: 'คำอธิบายสั้น ๆ',
  level: 'Beginner' // หรือ 'Intermediate', 'Advanced'
}
```

### 🔥 เปิดใช้ Firebase (บันทึกฟอรั่มลง Cloud)

1) สร้าง Firebase Project และ Firestore (โหมด Production หรือ Test ก็ได้)
2) ไปที่ Project settings > Your apps > Web app แล้วคัดลอกค่าคอนฟิก
3) คัดลอกไฟล์ `.env.example` เป็น `.env.local` แล้วเติมค่า:

```
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
VITE_FIREBASE_PROJECT_ID=...
VITE_FIREBASE_STORAGE_BUCKET=...
VITE_FIREBASE_MESSAGING_SENDER_ID=...
VITE_FIREBASE_APP_ID=...
```

4) ติดตั้ง SDK:

```bash
npm install firebase
```

5) รันแอป:

```bash
npm run dev
```

หมายเหตุ: ถ้าไม่ได้ตั้งค่า Firebase แอปจะ fallback เก็บโพสต์ในหน่วยความจำ (หายหลังรีเฟรช)

---

## 📊 เนื้อหาและข้อมูลตัวอย่าง

### Module
- **Module 1**: บทนำสู่ Micro-learning (15 นาที)
- **Module 2**: การสร้าง LMS ฟรีเมียม (20 นาที)
- **Module 3**: การพัฒนา Quiz และ Assessment (25 นาที)
- **Module 4**: Gamification และ Engagement (18 นาที)

### Quiz Questions
- 5 ข้อเกี่ยวกับ Micro-learning, LMS, Client-Side Validation, Freemium Model, และ React State

### Video URLs (ตัวอย่าง)
ใช้ YouTube Embed URLs จาก:
- `https://www.youtube.com/embed/dQw4w9WgXcQ`
- `https://www.youtube.com/embed/jNQXAC9IVRw`
- เป็นต้น

---

## 🔧 วิธีปรับแต่งและขยายฟีเจอร์

### 1. เปลี่ยนปริมาณการเลือกคำตอบใน Quiz

แก้ไขใน [src/components/QuizComponent.jsx](src/components/QuizComponent.jsx):
```javascript
// ลบหรือเพิ่มตัวเลือกใน options array
options: [
  'ตัวเลือก A',
  'ตัวเลือก B',
  'ตัวเลือก C',
  'ตัวเลือก D'
  // เพิ่มตัวเลือก E ได้
]
```

### 2. เปลี่ยนจำนวน Module

แก้ไขใน [src/components/ModuleStructure.jsx](src/components/ModuleStructure.jsx):
```javascript
const modules = [
  // เพิ่มหรือลบ Module
  {
    id: 5,
    title: 'Module 5: ...',
    // ...
  }
];
```

### 3. เปลี่ยนสี Theme

แก้ไขใน CSS files ทั้ง 3 ไฟล์:
```css
/* เปลี่ยนจาก */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* เป็น */
background: linear-gradient(135deg, #FF6B6B 0%, #ee5a6f 100%);
```

### 4. เปลี่ยนวิดีโอ YouTube

แก้ไข `videoUrl` ใน Component:
```javascript
// จาก
videoUrl: 'https://www.youtube.com/embed/dQw4w9WgXcQ'

// เป็น
videoUrl: 'https://www.youtube.com/embed/YOUR_VIDEO_ID'
```

---

## 🌐 ขั้นตอนการปรับใช้ (Deployment)

### Deploy ไป GitHub Pages

```bash
# 1. Build โครงการ
npm run build

# 2. Push ไป GitHub
git add .
git commit -m "LMS Prototype v1.0"
git push origin main

# 3. ตั้งค่า GitHub Pages ในการตั้งค่า Repository
# - ไปที่ Settings > Pages
# - เลือก Branch: main, Folder: /docs
# - หรือใช้ GitHub Actions ด้วย
```

### Deploy ไป Netlify

```bash
# 1. Build โครงการ
npm run build

# 2. ลากและวาง dist/ folder ไปที่ Netlify
# หรือเชื่อมต่อ Repository และ Deploy อัตโนมัติ
```

### Deploy ไป Vercel

```bash
# 1. ติดตั้ง Vercel CLI
npm install -g vercel

# 2. Deploy
vercel
```

---

## 🐛 Troubleshooting

### ❌ ปัญหา: "npm install" ไม่สำเร็จ

**วิธีแก้:**
```bash
# ลบ node_modules และ package-lock.json
rm -rf node_modules package-lock.json

# ติดตั้งใหม่
npm install
```

### ❌ ปัญหา: วิดีโอ YouTube ไม่แสดง

**วิธีแก้:**
1. ตรวจสอบว่า iFrame src ถูกต้อง
2. ตรวจสอบว่า YouTube อนุญาตให้ Embed
3. ตรวจสอบ Browser Console เพื่อดู Error

### ❌ ปัญหา: CSS ไม่โหลด

**วิธีแก้:**
1. ตรวจสอบว่า path ไฟล์ถูกต้อง
2. ตรวจสอบความถูกต้องของ CSS Syntax
3. ลบ Cache Browser: Ctrl+Shift+Delete

---

## 📱 Browser Compatibility

ทดสอบแล้วใน:
- ✅ Chrome/Edge (เวอร์ชันล่าสุด)
- ✅ Firefox (เวอร์ชันล่าสุด)
- ✅ Safari (เวอร์ชันล่าสุด)
- ✅ Mobile Browsers

---

## 📚 อ้างอิงและทรัพยากร

### React Documentation
- [React Official](https://react.dev)
- [React Hooks](https://react.dev/reference/react/hooks)
- [useState Hook](https://react.dev/reference/react/useState)

### CSS & Design
- [CSS Gradients](https://developer.mozilla.org/en-US/docs/Web/CSS/gradient)
- [Flexbox Guide](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [Grid Guide](https://css-tricks.com/snippets/css/complete-guide-grid/)

### LMS Best Practices
- [SCORM Standard](https://en.wikipedia.org/wiki/SCORM)
- [Instructional Design](https://www.instructionaldesign.org/)
- [Micro-learning](https://en.wikipedia.org/wiki/Microlearning)

---

## 🎓 บทบาทการพัฒนา

### สำหรับ Frontend Developer
- ออกแบบ UI/UX ที่สวยงาม
- ปรับปรุง Responsive Design
- เพิ่มประสิทธิภาพ Performance

### สำหรับ Backend Developer
- สร้าง API สำหรับบันทึกข้อมูล Quiz
- พัฒนา Database Schema
- สร้าง Authentication & Authorization

### สำหรับ Content Creator
- เตรียมคำถาม/คำตอบใหม่
- สร้างวิดีโอบทเรียน
- เขียนคำอธิบายและ Explanation

---

## 🤝 การมีส่วนร่วม (Contributing)

ต้องการช่วยพัฒนา LMS ต่อไปหรือไม่?

```bash
# 1. Fork Repository
# 2. สร้าง Branch ใหม่
git checkout -b feature/your-feature

# 3. Commit ข้อมูล
git commit -am 'Add some feature'

# 4. Push ไป Branch
git push origin feature/your-feature

# 5. สร้าง Pull Request
```

---

## 📞 ติดต่อและสนับสนุน

หากมีปัญหาหรือมีคำถาม:
- 📧 Email: contact@example.com
- 💬 Discord: [Link]
- 📱 Facebook: [Link]

---

## 📄 ใบอนุญาต (License)

โครงการนี้ใช้ใบอนุญาต MIT License - ดูไฟล์ [LICENSE](LICENSE) สำหรับรายละเอียด

---

## 🎉 ขอบคุณ

ขอบคุณที่ใช้ LMS Prototype นี้! เราหวังว่าจะช่วยให้การเรียนการสอนของคุณมีประสิทธิภาพมากขึ้น 🚀

**Created with ❤️ for Education**

---

## 📈 Roadmap (แผนการในอนาคต)

- [ ] เพิ่มระบบ User Registration & Login
- [ ] สร้าง Database เพื่อบันทึกความคืบหน้า
- [ ] เพิ่มระบบ Notification
- [ ] สร้าง Admin Dashboard
- [ ] เพิ่ม Certificate Generation
- [ ] สนับสนุน Video Upload
- [ ] สร้าง Mobile App
- [ ] Multilingual Support (EN, TH, etc.)

---

**Version:** 1.0.0  
**Last Updated:** December 2024  
**Status:** 🟢 Active Development